{
  "name": "aiatella-form-handler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that receives the AIATELLA waitlist and contact form posts",
  "scripts": {
    "test": "node --test \"test/*.test.mjs\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
